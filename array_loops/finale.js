// access the second value of this array
//   remember: arrays start at 0
let groceries = ['apples', 'bananas', 'pineapples', 'oranges']

// use a 'for' loop to log every value of this array to the console
//   remember the 3 components of a for loop

let counter = 5
// using a 'while' loop, decrement this counter variable until it equals 0, and log it to the console

let citiesInCalifornia = ['Los Angeles', 'San Francisco', 'Reedley']
// using Array.map() add the proper state to the end of each of these cities - they're all in CA
//   ie - 'Los Angeles, CA', etc

let numbers = [5, -10, 80, 67, 3, 0]
// sum these values using Array.reduce()

// using Array.filter(), filter out all numbers that are greater than 10

let modifyMe = ['First', 'Second', 'Third']
// Push, Pop, Shift, Unshift
// add the following value to the end of the 'modifyMe' array
let pushMe = 'I got pushed'

// now remove the value from the end of the array

// remove the first value from the modifyMe array

// now add this value
let newFirst = 'New First'

// Objects
let meObjectified = {
  name: 'Chris',
  isTall: true,
  height: 75
}

// access the 'name' value using dot notation

// access the 'height' value using bracket notation

// return an array of all the keys of the above object

// check if the above object has an 'age' property
//   hint: Object.hasOwnProperty()

// stringify the above object using JSON

// now parse it back